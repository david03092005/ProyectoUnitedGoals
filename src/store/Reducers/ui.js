
const EstadoInicial = {
    showModal: true
};

const uiReducer = (estado = EstadoInicial, accion) => {
    switch(accion.type){

        default:
            return estado;
    }
};

export default uiReducer;